contourpy==1.3.0
cycler==0.12.1
fonttools==4.60.2
importlib_resources==6.5.2
joblib==1.5.3
kiwisolver==1.4.7
matplotlib==3.9.4
numpy==2.0.2
packaging==26.0
pandas==2.3.3
pillow==11.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2025.2
scikit-learn==1.6.1
scipy==1.13.1
seaborn==0.13.2
six==1.17.0
threadpoolctl==3.6.0
tzdata==2025.3
zipp==3.23.0

# Phase 2 (EDA & preprocessing)
statsmodels==0.14.4   # univariate tests, FDR correction
# ComBat batch correction is NOT pinned here. `inmoose` needs a C toolchain and fails
# to build a wheel on Windows without one, and its failure aborts the whole install.
# It is only needed if the PHASE batch check exceeds the 10% variance threshold --
# defer the choice of implementation until that check has actually run.
